<%- include('partials/header'); %>

    <h1>
        first ejs file
    </h1>

    <% for(var i=0;i<posts.length;i++){%>

        <ul>
            <li>
                <%= posts[i].post %> -
                    <%= posts[i].author %>
            </li>
        </ul>

        <%   } %>

            <%- include('partials/footer'); %>